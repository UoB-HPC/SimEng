#pragma once

namespace simeng {
class SpecialFileDirGen {
 public:
  /** Construct a SpecialFileDirGen class by reading in the YAML file and
   * running it through checks and formatting. */
  SpecialFileDirGen(std::string path);

 private:
}
}  // namespace simeng